# Для корректной работы бота нужны:
## Установка требуемых инструментов
```shell
pip install -r requirements.txt
```
## Создание файла .env
```
TOKEN=[токен телеграм бота]
DB_URL=sqlite+aiosqlite:///users.db
ADMINS=[ID админов через запятую без пробелов ]
```