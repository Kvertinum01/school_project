<p>Для корректной работы бота нужны:</p>
<p>Установка требуемых инструментов</p>
```shell
pip install -r requirements.txt
```
<p>Создание файла .env</p>
```
TOKEN=[токен телеграм бота]
DB_URL=sqlite+aiosqlite:///users.db
```